// put to the index file, export of all the things that will be needed in other part of app - thanks to this need to ref only to index

export { fetchEvents } from './events';
export { fetchEvent } from './events';